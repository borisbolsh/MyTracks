import UIKit

enum Icons {
	enum Tab {
		static let tasks = UIImage(named: "time-outline")
		static let addTask = UIImage(named: "add")
		static let statistics = UIImage(named: "pie-chart")
	}

	static let search = UIImage(systemName: "search.fill")
}
